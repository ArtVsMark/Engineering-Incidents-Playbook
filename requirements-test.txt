# Зависимости ТЕСТОВ, в одном месте.
#
# Их ставят две работы — ci.yml и badges.yml, — и до этого файла список был
# вписан в обе. Два списка одного набора расходятся при первой же правке
# (правило 022), и разошлись бы молча: работа, которой чего-то не хватает,
# падает на первом же импорте, но у ДРУГОЙ работы при этом всё зелено.
#
# ИМЯ ПАКЕТА И ИМЯ МОДУЛЯ РАЗНЫЕ — и второе объявляется здесь же, комментарием
# «# модуль: имя». Спрашивать метаданные установленного пакета нельзя: гейт
# тогда зависит от того, что уже установлено, то есть болеет ровно той
# болезнью, которую ловит. Замер: первая версия гейта прошла у автора и упала
# в конвейере, где библиотеки на момент проверки ещё нет.
#
# ЧЕГО ЗДЕСЬ НЕТ НАМЕРЕННО: зависимостей самих скриптов. У них их нет ни одной,
# и это осознанно — разбор YAML в sync_labels.py написан руками ровно затем,
# чтобы не тянуть библиотеку в конвейер потребителя. Здесь другое дело: тесты
# у потребителя не запускаются.
pytest
coverage
# Собранный рабочий процесс обязан разбираться как YAML — иначе потребитель
# получит файл, который площадка не примет. Проверка этого стоит библиотеки:
# писать свой разбор YAML ради одного случая было бы хуже (задача про #151:
# тест зеленел у автора и падал в конвейере — правило 107).
pyyaml  # модуль: yaml
