Прогон `consumers-sync` красный: ответ объявленного потребителя изменился, а сводка в репозитории осталась прежней.

Починка — одна команда, и её результат надо закоммитить:

```
python scripts/aggregate_bindings.py
```

Задача заведена автоматически и **только один раз**: пока она открыта, следующие прогоны молчат. Закройте её вместе с починкой — иначе следующее расхождение останется без адресата (правило 142).
