Гейт 167 перестал заглядывать в прозу: ключи YAML ищутся образцом «отступ,
слово, двоеточие», и под него подходила любая строка текста внутри блочного
скаляра. Строка «ВЕРДИКТ: находок N» в промпте ревьюера была принята за имя
ключа env и отвергнута как «не латиницей» — гейт краснел на верном файле.
Содержимое `run:` по-прежнему разбирается: там код, и присваивание кириллицей
падает кодом 127.
