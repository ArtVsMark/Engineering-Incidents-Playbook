Ссылки на правило 022 в коде сверены с его применимостью (204): 022 — о документации, а в коде его цитировали 115 раз, и 98 стояли мимо. Сорок переведены туда, куда ведёт применимость: 035 (значение, дублирующееся по необходимости: имя репозитория, имя навыка, соответствие «пакет → модуль», «путь → зона»), 209 (словари, которые механизм узнаёт побуквенно), 090, 049, 122, 094, 160, 196. У пятидесяти восьми номер снят, текст оставлен: они обосновывают «одна логика — одна реализация» в коде, а такого правила в каталоге нет. Спорное решалось в сторону снятия — неточная ссылка хуже отсутствующей. Семнадцать остались: там 022 в своей области.

Несущее объявление переехало вместе с ответом: `check_own_name.py` держит не 022, а обобщение 035 на имя продукта, и в `.rules/bindings.json` он назван теперь у 035, а у 022 остался `check_charter.py`.

> правилом не становится, потому что это 204 на цитатах кода: новой поломки нет. Пробел «одна логика — одна реализация» записывается отдельно.
