«Без отвергаемого предмета» перестало быть метрикой и стало отказом. У каждого
гейта конвейера обязан быть предмет, который он отвергает: подделка в
`check_gates.py`, случай в наборе, где гейт отвечает **отказом** через `main()`,
либо собственная самопроверка, которую **зовёт** конвейер. Список освобождённых
разрешительный и сейчас пуст.

Приём взят у витрины `ArtVsMark`: её `check_mechanisms.py::audit_voice` требует
отрицательный набор у каждого скрипта, способного отвергнуть, а `::audit_harness`
— чтобы конвейер этот набор звал. Увидели через раздел «У соседей это уже
решено», добавленный во входящие этим же днём.

Засчитывается именно **случай с отказом**, а не наличие файла набора: 31 августа
два набора были написаны так, что звали внутреннюю функцию, а не `main()`, — файл
существовал, а мутация «главный ход её не зовёт» их переживала (150).

Долг, который это вскрыло, закрыт тем же изменением. У `check_labels.py`
самопроверка была написана и **не запускалась ничем** — конвейер её не звал; у
`check_gates.py` не было набора вовсе, и собственная его поломка не ловилась
никак. Набор к нему проверяет три тихие поломки: вердикт посреди перебора (159),
третий исход, слитый со вторым (039), и находку без предмета в выводе (158).
