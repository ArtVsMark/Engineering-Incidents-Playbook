Вердикт о корне репозитория перестал опираться на чужой ответ: его довод
считал упоминания `README.en.md` в `export/where.json` и выводил «назван
трижды», а наше там только одно — замер предмета 196; два других принадлежат
срезу потребителя `ArtVsMark/Claude-Code_Usage-Token` и говорят о ЕГО
собственных README. Сводка склеивает ответы всех потребителей, и счёт по ней не
есть счёт по нам; сам файл вдобавок в общей ветке не лежит — он производное,
публикуемое прогоном на ветку `badges`, и в дереве виден только после локальной
сборки, что и сделало ошибку незаметной для автора и невоспроизводимой для
читателя. Довод заменён на проверяемый чтением кода: `README.en.md` привязан
ПУТЁМ в четырёх гейтах (`SHOWCASES`, `MARKERS`, `EN_DOCS`, перечисление в
`check_prose.py`) плюс языковым переключателем `README.md:15`, а у `TOP-10.md`
машинной привязки по пути нет вовсе. Вывод «бесспорного кандидата на переезд
нет ни одного» от замены устоял — сменилось основание, а не он.

> правило 190 — это его случай: форму и содержание чужого ответа спрашивают у
> соседа, а не помнят и не читают как своё. Своей записи класс не требует
