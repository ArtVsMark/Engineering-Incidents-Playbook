Возражение о форме приехавшего предложения снимается ВЫНЕСЕННЫМ РЕШЕНИЕМ, а не
повторяется вечно. Проверка полей стояла ДО проверки «решение уже вынесено», и
разобранное предложение возражало каждый заход: чужой файл мы не правим, а
вердикт у нас записан. Замер 8 сентября: `repair-acceptance-stronger-than-defect`
грейдера принят правилом 193 ещё накануне — и всё равно печатался в живой
задаче как непрочитанный. Очередь показывала работу, которой нет (075).

СООБЩЕНИЕ ПРИ ЭТОМ НАЗЫВАЛО НЕ ТУ ПРИЧИНУ. Стояло «номер присваивает каталог
при приёме, не отправитель» — утверждение о том, что отправитель пытался
присвоить номер. У грейдера в поле `rule` лежит ТЕКСТ УТВЕРЖДЕНИЯ: имена полей
у нас и у него разошлись, и это единственная беда. Теперь сообщение называет оба
случая и подсказывает верные имена — `claim`, `incident`, `trail`, — вместо того
чтобы обвинять в чужом намерении (183).

Порядок здесь несущий, а не стилистический: форма важна ДО решения, после —
решение и есть ответ. Набор бьёт в оба конца: предложение с полем `rule` без
вердикта остаётся находкой, оно же с вердиктом — нет; негодный слаг возражает
раньше всякого решения, потому что без него ключа вердикта не построить вовсе.

Подделка сети названа источником (170): адрес входящих предложений живёт у
каждого потребителя в `.rules/consumers.json`, поле `proposals`.

> правило 075 — не нашёл предмета, падает, а не зеленеет: здесь обратное того
> же рода, гейт нашёл предмет, которого нет, и держал очередь непустой. Новой
> записи случай не требует; 183 покрывает вторую половину — сообщение
> утверждало о чужом намерении то, чего в данных не было
