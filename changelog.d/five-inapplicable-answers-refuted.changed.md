Пять ответов «неприменимо» прочитаны поимённо против ответа соседнего проекта (#514), и ни один не выдержал: у каждого буква или обобщение живут в нашем дереве (184). Три правила уже держались машиной, которая их не называла, — теперь названо: 019 держит `scripts/check_readers.py` (у каждого читателя из словаря есть документ — пустая клетка видна без обхода файлов), 059 — `scripts/check_transport.py` (что встаёт при исчерпании самого узкого счётчика площадки, закрыто списком), 078 — `scripts/main_red.py` (отмена не вердикт и не красное; мутация без `cancelled` роняет самопроверку). Два встали долгом «держится ничем», и это честнее прежнего: 032 — замер по #521–#570 дал 8 вердиктов ревьюера из 45, вынесенных чтением, потому что прогон ему разрешён, а возможности нет (ни setup-python, ни установки проверок в работе); 076 — задача-входящие у потребителя несёт 78 строк с голым путём чужого дерева, а заготовки — 37 ссылок `](../rules/…)`, живых только внутри каталога.

> правилом не становится, потому что это исполнение 184 и 205: ответ «неприменимо» разбирается надвое и держится предикатом по дереву, и у всех пяти предикат его опроверг. Новой поломки нет — есть пять ответов, которые спрашивали не тот предмет.
