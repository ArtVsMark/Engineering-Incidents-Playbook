**014 объявлял невозможной всю проверку, а половин две** (014, 182,
`.rules/bindings.json`).

Ответ стоял с `document_reason: "impossible"` и доказывал ровно одно: отличить
в откате ПОВЕДЕНИЕ от ИМЁН машина не может. Это верно и остаётся в записи —
одна и та же удалённая строка бывает и тем и другим, а `ImportError` вместо
падения по существу узнаёт только тот, кто знает, какого падения ждал.

Неверен вывод. Полу-откатом доказывают красноту **проверки**, а если починка
проверки не принесла, доказывать нечем вовсе. Это вторая половина правила, и
она машинная: «тронул `scripts/*.py` — принеси `tests/`». У соседа она
построена и закреплена двумя случаями, на отказ и на пропуск.

ЧИСЛО СКАЗАНО ДО РЕШЕНИЯ, А НЕ ПОСЛЕ. Замер 16.09 по сорока последним коммитам
общей ветки: починок **29**, из них тронувших `scripts/*.py` — **11**, проверку
принесли **7**, не принесли **4**. Четыре разобраны поимённо, и они не одно и
то же:

| починка | что менялось | проверка |
|---|---|---|
| `801583e` | только докстрока | не нужна |
| `5bea045` | только докстрока | не нужна |
| `99e21a2` | новая ветка ОТКАЗА в `scripts/check_exclusive.py` | нет и сегодня — это ещё и 140 |
| `d7cda04` | разбор в `scripts/refresh_derived.py`, 143 строки | файла проверок у скрипта нет вовсе |

Итог честен в обе стороны: гейт **с освобождением прозы** отверг бы **два**
изменения из одиннадцати, и оба отказа были бы верны; **без освобождения** —
четыре, и два из них ложно. У соседа тот же замер дал пятнадцать из пятнадцати:
он закреплял соблюдаемое, мы закрывали бы два настоящих пропуска.

Поэтому здесь только перечёт: `impossible` → `not-yet`, и в `where` названа
вторая половина. Сам гейт и требование к участнику в `CONTRIBUTING.md` —
предложение владельцу, а не правка по ходу.

> правилом не становится, потому что предмет держат 182 — ответ «машина не держит»
> разбирается надвое — и 039: третий исход есть, и он «половина есть, но не
> построена», а не «невозможно». Новой поломки нет: есть ответ, доказавший
> одну половину и назвавший этим доказательством обе. Третья запись такой формы
> после 047 и 082 (#508): под словом «невозможно» лежит доказательство ровно
> одной половины. 006 (#511) сюда не считается — там довод был просто неверен,
> а не узок.
