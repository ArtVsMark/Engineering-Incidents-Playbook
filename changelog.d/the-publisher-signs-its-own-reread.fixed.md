Каталог перечитал **собственные** ответы и подписался под этим: `answers_to`
в `.rules/bindings.json` и в заготовке для потребителей сдвинут `1.5 → 1.7`.
Отставание было в два подъёма — выгрузка ушла на 1.6 (буква и граница записи)
и 1.7 (взаимосвязи), а подпись стояла на 1.5. Ровно та асимметрия, из-за
которой заведён `check_reread.py`: издатель освобождает себя от требования,
которое разослал пятерым. Гейт её не видел по построению — он краснеет, когда
подпись ДВИГАЕТСЯ без названного обхода, и молчит, когда она стоит, а контракт
уехал. Отставание при этом не было невидимым: `aggregate_bindings` печатает
его по каждому потребителю, включая нас, — но ночным прогоном, а не заслоном.

Перечитывание сделано, а не объявлено, и его предмет назван числами. Всего
ответов **195**. Два механических среза по всему корпусу: близость заголовка к
букве (медиана 0,075 — то есть заголовок в принципе букву не передаёт, ради
чего 1.6 и заведена) и ответы, где механизм назван «документ» или «ничто», а
адрес указывает на исполняемое. Прочитано целиком **19** ответов: десять с
худшей близостью заголовка к букве (004, 006, 022, 045, 050, 055, 072, 090,
097, 005), пять со срезом «документ, а адрес исполняемый» (050, 101, 132, 149,
187) и четыре, чьё чтение изменила именно 1.7 — пара уточнений 057 → 002 и
149 → 103.

Изменений в ответах нет, и это исход, а не отписка: 101 и 149 переотвечены ещё
при перечитывании по 1.5 и несут отдельное поле `machine_half`, 050 честно
делит предмет надвое — арифметику держит гейт, направление движения числа
читается человеком (182). Единственное, что сделала видимым 1.7: 103 у нас
`not-applicable` (сторожа побочных эффектов нет), а уточняющее его 149 —
`active`, потому что владение общей площадкой действует и без сторожа. Пара
согласована, противоречия нет.

> правилом не становится, потому что предмет уже держится 157 и его механизмом
> `check_reread.py`; здесь вскрылась не новая поломка, а ГРАНИЦА этого гейта —
> он судит движение подписи, а не её отставание. Расширять его на «подпись
> стоит, а контракт уехал» нельзя тем же способом: отставание законно ровно до
> тех пор, пока перечитывание не сделано, а сделано оно или нет, из дерева не
> следует (182). Механизм для второй половины уже есть и работает — ночной
> `aggregate_bindings` печатает отставание с адресатом; недостающее здесь —
> решение владельца, поднимать ли это до заслона.
