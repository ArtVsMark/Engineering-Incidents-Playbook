Реестр потребителей знает адреса `ArtVsMark/claude-code-usage`: `bindings` и
`proposals` держали `null`, хотя оба файла лежат в проекте с 22 августа. Пустое
поле означает «канал не подключён» — и сводка каталога читала подключённого
потребителя как неподключённого, а его предложения не видела вовсе.

> правилом не становится, потому что это данные реестра, а не устройство:
> механизм подключения уже есть, здесь просто не доехала вторая его половина
