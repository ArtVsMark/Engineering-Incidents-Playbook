Отказ записи в `link_trails` — третий исход, а не «записано». Запись обратной ссылки проверялась только на отсутствие `gh` и квоты, и отказ площадки с кодом 1 читался успехом: остаток после `--apply` выходил пустым при недописанной ссылке. Ответ на постановку метки не читался вовсе. Теперь обе записи, как и чтение, считают отказом любой ненулевой код и называют причину.

Соседи: это вторая находка по одному месту — обработке отказа площадки в `link_trails`, — и по правилу 210 перечислены все пять вызовов площадки в файле: чтение комментариев (починено прежней правкой), запись комментария и метки (здесь), два вызова самопроверки, где различие «инструмента нет» и «отказ площадки» и есть предмет. По всему дереву брошенный результат вызова площадки был один — метка.

> правилом не становится, потому что это 039 и 210 на свежей правке: новой поломки нет.
