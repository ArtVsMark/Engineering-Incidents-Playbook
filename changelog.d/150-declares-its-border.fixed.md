Ответ каталога по правилу 150 назвал свою границу. Он обещал, что «тест
спрашивает решение механизма» держится гейтом `check_gates.py`, — и это правда
ровно для набора подделок в самом гейте. Наборы в `tests/` он не смотрит.

Замер: 31 августа правило нарушено там дважды за одну смену. Новый случай звал
внутреннюю функцию вместо `main()` — сперва `check_attribution.tail`, затем
`check_showcase.badges_in_tree`, — и оба раза мутация «главный ход её не зовёт»
набор пережила. Поймала обе мутация, а не гейт.

> правило 150 — та же поломка, о которой оно и написано, но в наборе, а не в
> гейте: декларация обещала покрытие шире фактического. Новой записи не
> требует; требует честной границы, что и сделано
