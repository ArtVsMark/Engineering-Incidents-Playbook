Контракт предложений `1.1 → 1.2`: тем же каналом снизу проект предлагает навык — `kind: skill`, путь к `SKILL.md` у себя, полный `sha` коммита, `holds` (правила каталога, которые навык держит), `measurement` (замер в работе вместо инцидента) и `amends` (навык каталога, который он дорабатывает). `scripts/collect_proposals.py` читает текст на названном коммите, а не на ветке, сверяет заголовок `SKILL.md` тем же разбором, что и ответ о навыке (`check_bindings.поля_навыка`, 214), и кладёт навык в ту же очередь; вердикт навыку — ключ `владелец/репозиторий:skill/слаг` с полем `skill`, и гейт требует, чтобы принятый навык лежал в `.claude/skills/`. Без `kind` предложение — правило, как в `1.1`. Перечитано 49 вердиктов каталога, изменено 0. `scripts/onboard_consumer.py` перестал вписывать номер `1.0` руками: номера обоих файлов берутся из блока `contracts` выгрузки.
