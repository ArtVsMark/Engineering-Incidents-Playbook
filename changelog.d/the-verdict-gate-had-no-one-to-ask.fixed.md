Гейт «починка ответила, тянет ли она на правило» снова спрашивает у добавленных
фрагментов. Он звал `git diff --name-only -z`, а разбирал ответ по пробелам:
имя приезжало с хвостовым нулевым байтом, такого файла нет ни на каком диске, и
`verdict_problems` пропускал его первой же строкой — `not path.exists()`. Гейт
печатал «фрагменты журнала в порядке», не спросив НИ ОДНОГО. Замер 8 сентября:
из 38 починок 35 без разбираемого вердикта, то есть он не срабатывал ни разу с
заведения. Ключ `-z` просили ровно затем, чтобы имя с пробелом не разъехалось,
— `split()` отменял и это. Найдено внешним взглядом, воспроизведено прогоном
настоящей команды конвейера на диапазоне изменения #404: код 0 там, где обязан
быть 1. Набор бьёт в оба конца и в само место потери: имя обязано быть
существующим путём, починка без вердикта — находкой, починка с вердиктом —
нет, имя с пробелом не режется надвое.

> правило 146 — зелёный гейт не проверяет свою посылку: этот был зелен не
> потому, что фрагменты отвечали, а потому, что список, у которого он
> спрашивал, всегда оказывался пуст. Проверялась чистая функция, а не шов,
> которым её кормят
