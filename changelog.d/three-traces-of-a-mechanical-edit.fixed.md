**Три следа механической правки, ни один из которых не меняет поведения** (183,
022, `export/README.md`, `scripts/check_bindings.py`, `tests/`).

Подъём контракта `bindings` до 1.5 переводил 203 записи и правил шесть читателей
словаря. Работа вышла механической — и механическими вышли три её следа, которых
не поймал ни один прогон, потому что поведения они не меняют.

СТРОКА `WHY` В ТАБЛИЦЕ КОНТРАКТА ССЫЛАЛАСЬ НА СНЯТОЕ ПОЛЕ. Таблица говорила
«обязательно вместе с `document_reason`» тремя строками выше того места, где то
же поле объявлено устаревшим. Две строки одной таблицы утверждали разное про один
контракт; потребитель, читающий её буквально, связал бы обязательность `why` со
снятым именем, тогда как гейт требует его рядом с `holdable`.

ПОМОЩНИК НАБОРА НЁС ОДИН КЛЮЧ ДВАЖДЫ. В `ничем()` поле `holdable` стояло два раза
в одном литерале — след вставки, не перечитанной после. Значения совпадали, и
Python не падает.

ВТОРОЙ ПСЕВДОНИМ ОДНОГО МОДУЛЯ. `import datetime as _dt` встал рядом с уже
существующим `import datetime as dt` — один предмет под двумя именами в одном
файле (022).

> правилом не становится, потому что предмет держат 183 — проза о механизме есть
> утверждение о текущем коде — и 022, один предмет под одним именем. Новой
> поломки нет. Но замечание общее и стоит записи: ПРАВКА, НЕ МЕНЯЮЩАЯ ПОВЕДЕНИЯ,
> не проверяется ничем из того, что стоит. Прогон зелен, набор зелен, гейты
> зелены — и всё это верно при таблице, спорящей сама с собой. Нашёл внешний
> взгляд, и это ровно тот класс, ради которого канал ревизии и заведён (084).
