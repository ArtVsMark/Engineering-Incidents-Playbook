**«Возраста окна в дереве нет вовсе» — неверно: он в трейлерах** (006,
`.rules/bindings.json`).

Ответ на 006 стоял с `document_reason: "impossible"` и обосновывался так:
счётчика сессии площадка не отдаёт, значит сроку не с чем сверяться. Первая
половина верна, вывод — нет. Возраст окна в дереве **есть**: трейлер
`Claude-Session` датирует первый и последний коммит каждого окна, и счётчик от
площадки для этого не нужен вовсе.

Замер по общей ветке 16.09: **10 окон**, у двух срок превышен — **5,09** суток и
**8,33** суток. Второе на момент замера ещё работало.

Правило 006 даёт предел три–пять суток. То есть проверка не только строится — у
неё уже есть что называть, причём не задним числом: превышение видно на живом
окне в тот же день.

Человеку остаётся ровно то, что и должно: признаки, по которым не ждут
календаря — окно перечитывает прочитанное, противоречит себе внутри смены,
пересказ прошлого длиннее работы, — и само решение перезапустить. Шаг называет
срок числом и за человека не решает; так же устроен он и у соседа, где построен.

> правилом не становится, потому что предмет держат 182 — ответ «машина не
> держит» разбирается надвое — и 175: утверждение «предмета нет», опровергаемое
> одной командой, обязано быть гейтом. Здесь опровержение и есть одна команда
> `git log` по трейлерам, без сети. Новой поломки нет: есть ответ, отрицавший
> данные, которые лежат в том же дереве.

Соседи: это третья запись из семнадцати спорных (#509), прочитанная поимённо
после 047 и 082. Остальные четырнадцать не тронуты — по ним чтения ещё нет, а
правка по сигналу «у соседа гейт» была бы догадкой.
