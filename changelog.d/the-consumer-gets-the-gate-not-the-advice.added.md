Шаблон `templates/preflight.py` получил шаг «транспорт»: он находит вызовы `gh`
семейств, идущих через GraphQL, и требует либо REST, либо имени в закрытом
списке `.rules/transport.json`. До сих пор правило 001 доезжало до потребителя
текстом, а текст не исполняет никто. Квота у площадки одна на учётную запись,
поэтому предмет у всех общий.
