Адрес предложений витрины прописан в реестре потребителей: поле `proposals` у записи `ArtVsMark/ArtVsMark` было `null`, и обратный канал не читался никем — сколько бы предложений витрина ни положила у себя. Проверено на живом предмете: каталог видит два её предложения, замечаний ноль.

> правило 147 — тот же класс: отсутствующий адрес и пустой список дают один наблюдаемый исход. Каталог молчит одинаково, и «канал не подключён» неотличимо от «предлагать нечего». Новым правилом не становится, потому что 147 это уже покрывает: здесь не новая поломка, а её случай в реестре потребителей.
