**Снятие заметки внешнего взгляда проверяется машинно.** Механизм находок
уносит запись из живой задачи по строке `Разобрано: <отпечаток>` в теле слитого
изменения. Формы этой строки не проверял никто: `Разобрано: 202-boundary`
выглядело разобранным для человека и не совпадало ни с чем для машины.

**Инцидент — свой, 10 сентября.** Ровно так я и написал снятия в теле #475:
словами вместо семи шестнадцатеричных знаков. Поймало чтением, за минуты до
слияния, и только потому, что я пошёл сверять отпечатки с задачей руками. Не
пойди — заметки остались бы висеть на уже починенном, а тело выглядело бы
исполненным: тот же класс, что и `(#83)` в заголовке вместо `Closes` в теле,
из которого выросло 064.

**Отвергается только достоверное.** Строка, чей хвост не начинается с семи
шестнадцатеричных знаков, не совпадёт с записью НИКОГДА — какой бы ни была
живая задача, и это проверяется без сети. Вторая половина — отпечаток верный по
форме, но в задаче не значащийся, — не построена и названа: различить опечатку
в знаке и запись, снятую раньше, можно только спросив трекер, а этот гейт
наружу не ходит по построению; там же и цена ошибки другая — снятие лишнего
безвредно, отказ на нём был бы ложным (051, 182).

**Замер перед постройкой: 58 слитых изменений, строк снятия 26, все 26
отпечатком.** Прозы, которую проверка задела бы, в корпусе нет ни одной — и это
не «риска нет», а «риск измерен»: собственный промах в корпус не попал только
потому, что я поправил тело через API до слияния.

**Форма живёт в одном месте.** Выражение объявлено у гейта тела —
`pr_body.РАЗОБРАНО`, — и уборщик `review_findings` берёт его оттуда, а не пишет
второй раз. Разойдясь, они молчали бы в разные стороны: гейт пропускал бы
строку, которую уборщик не примет (022). Совпадение проверяется случаем.

> правилом не становится, потому что предмет держат 064 — освобождение
> заполняется, а не пропускается, и форма связи проверяется машинно — и 002:
> требование, которое нельзя проверить машинно, соблюдаться не будет. Новой
> поломки нет: есть механизм, чью форму никто не спрашивал.

Соседи: спрошены все три места, где тело изменения разбирается. `pr_body.build`
собирает тело при открытии и о снятиях не судит — отказ там запрещён отдельно
(147); `check_task_state` смотрит на задачу ПОСЛЕ слияния, у него другой
предмет; `push_guard` вызывает эту же проверку до толчка и получает новый отказ
даром. Спрошен и `sync_inbox`: строка «Разобрано здесь: N» — печать счётчика, а
не снятие, и под выражение не попадает (проверено).
