**Сто девяносто восемь записей прочитаны поимённо и получили дату сверки**
(контракт `bindings` 1.5, `.rules/bindings.json`).

Поля `analysed` и `decided` завелись накануне, и датированы были четыре записи —
те, что разбирались по ходу. Остальные 198 стояли без даты, то есть с честным
ответом «не сверяли». Теперь сверены все 203.

ЧТО ЗНАЧИТ «СВЕРЕНА», И ГРАНИЦА НАЗВАНА СРАЗУ. По каждой записи рядом клались
три текста: строка «Правило» из записи каталога, объявление механизма в самом
механизме и наш ответ. Проверялось, что они об одном. Это НЕ прогон механизма и
не доказательство, что гейт держит заявленное, — такое доказательство даёт набор
и конвейер. Здесь проверено, что ответ описывает то же, что делает механизм.

`DECIDED` НЕ ДВИНУТ НИ У ОДНОЙ. Ни один вердикт сверка не сменила — а дата
вердикта двигается только вместе с ним. Ровно для этого две даты и разведены:
«перечитали и подтвердили» теперь отличимо от «решили заново».

ЧТО НАШЛОСЬ. Машинных записей 133; у **одиннадцати** ни один скрипт и ни один хук
не объявляет правило своим. Шесть из них законны — механизм там прогон, хук или
набор, а не `scripts/*.py`: 063, 097, 131, 147, 148 и 111. Пять требуют решения
поимённо:

| запись | что в ответе | вопрос |
|---|---|---|
| 003 | свод называет префикс ветки переключателем | это документ, а не гейт: что проверяет отказ? |
| 011 | прогон ходит по расписанию, а не циклом | конфигурация, а не проверка |
| 071 | намеренный повтор подписан комментарием | подпись никто не сверяет |
| 091 | свод перечисляет источники работы по порядку | документ под словом `gate` |
| 189 | назван хук `push_guard.py` | хук правило своим НЕ объявляет — дыра в гейте 183 |

Обратный случай один: **050** объявлен `document`, а адрес называет
`scripts/check_schedules.py` — машинная половина есть и названа.

ВЕРДИКТЫ ПО НИМ НЕ ВЫНЕСЕНЫ ЗДЕСЬ НАМЕРЕННО. Пять записей — пять чтений, и
каждое двигает `decided`; свалить их в один росчерк значило бы сделать ровно то,
от чего предостерегает разбор надвое (182). Они названы числом и поимённо, и это
их состояние до решения (046).

ПОПУТНО ПОДТВЕРЖДЕНО СОБЫТИЯМИ ТОГО ЖЕ ДНЯ: запись 204 говорит, что применимость
цитируемого правила проверяется ЧТЕНИЕМ, а не признаком. Сегодня внешний взгляд
поймал именно так неверную ссылку на 195 — по разделу «Применимость», которого
машина не читает. Ответ не менялся; он получил свидетельство.
