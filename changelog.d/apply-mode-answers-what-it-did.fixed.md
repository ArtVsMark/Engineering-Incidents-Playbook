`link_trails.py` больше не отвечает одним кодом на две разные вещи: с `--apply`
удачная запись даёт ноль, а единица осталась за сухим прогоном, где находки так
и не устранены. Прежде `missing` считался до записи и после неё не очищался, и
читающий не мог отличить «нашёл и починил» от «нашёл и оставил» — прогон по
расписанию у потребителя красил успешную работу. Инцидент приехал предложением
из `Claude-Code_Usage-Token`.

> правило 186 — код возврата не меняет смысла между режимами: запись заведена
> ровно из этого предложения, и починка её и есть
