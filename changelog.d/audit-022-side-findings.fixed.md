Попутные находки обхода ссылок на 022, по одной на место. `check_badges_published.py` больше не называет 196 при пропуске чужого репозитория: «Применимость» 196 сама говорит, что чужой ресурс не её предмет, и ссылка на границу читалась как основание (находка внешнего взгляда). `consumers_picture.STATE` берёт слова состояния у `aggregate_bindings` — `NOT_CONNECTED`, `UNKNOWN`, — а не переписывает их буквами (209). Докстрока `coverage_badge.py` говорила, что значок лежит в общей ветке под гейтом свежести, — это описание пережило переезд значков на ветку `badges` (160) и теперь сказано, как есть. В `collect_proposals.py` снят повтор комментария над импортом.

> правилом не становится, потому что это 204 и 209 на свежих местах: новой поломки нет.

Соседи: признак не сужен — словарь `STATE` тот же, два ключа и два значения; изменилось только то, что ключи берутся константами сборщика, а не буквами. Читает его одна функция отрисовки в том же файле, и набор отрисовки проходит без правок.
