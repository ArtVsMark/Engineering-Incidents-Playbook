Четыре гейта каталога получили отвергаемый предмет: `check_links`, `collect_changelog`, `check_bindings` и `aggregate_bindings` научились ключу `--root`, и `check_gates.py` собирает им подделанные деревья. Наборы двусторонние — рядом с предметом, который гейт обязан отвергнуть, стоит здоровый. Метрика свода сдвинулась с «подделкой проверено 6, только запуском 5» на «10 и 1», непокрыт остался сам `check_gates`.
