Вердикт навыку в `.rules/proposals.json` сверяется тем же кодом, что вердикт правилу: `scripts/collect_proposals.py --check` держал для навыка свою ветку, и в ней не было проверки, которая у правила есть, — два `admitted` на один навык каталога проходили молча, тогда как два `admitted` под одним номером правила ловятся. Теперь ветка одна, и различается только цель — номер или навык; второе предложение, влитое в уже принятый навык, отвечается `merged-into` с причиной. Ветки «нет поля `skill`» и «нет причины» получили свои случаи в наборе.

Соседи: вердикт правилу проверяется прежним набором условий — номер обязателен, есть в `rules/ru`, у двух `admitted` один номер — находка, причина у `rejected` и `merged-into`; поменялся только текст находки о занятом номере, `num` ушёл из строк вместе с веткой навыка. 49 вердиктов каталога гейт проходит как и до правки.

> правилом не становится, потому что это исполнение 214 на собственном коде: две ветки одного вопроса разошлись ровно так, как правило предсказывает.
