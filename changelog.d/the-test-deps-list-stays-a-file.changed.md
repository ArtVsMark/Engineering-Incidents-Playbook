Список зависимостей тестов остаётся файлом: вторым вопросом задачи #341 было
«сложить `requirements-test.txt` в `pyproject.toml`», и отказ записан там же, в
манифесте, рядом с прежним решением — отвергнутое без причины возвращается
следующей ревизией (026) и уже вернулось, разбором самой задачи. Довода «один
источник вместо двух» не существует: секции зависимостей в манифесте нет
вовсе, список ровно один, и 022 говорит за нынешнее устройство. Файл при этом
не только список — в нём живёт соглашение «# модуль: имя», которое разбирает
`check_test_deps.py`. Первая редакция этой записи называла двух разбирающих;
внешний взгляд показал, что `check_runtime_deps.py` комментарий отбрасывает и
держит собственный словарь имён, — настоящий дубликат ответа нашёлся, только в
другом месте, и заведён задачей #444.
