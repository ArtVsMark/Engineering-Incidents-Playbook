«Какой модуль даёт этот пакет» отвечается один раз, а не дважды:
`check_runtime_deps.py` больше не держит собственный словарь
`ИМЕНА = {"pyyaml": "yaml", …}`, а зовёт разбор объявления
`check_test_deps.объявлено` — тот самый, что читает строку «# модуль: имя»
рядом с пакетом в `requirements-test.txt` (#444). Сегодня оба ответа совпадали,
и расхождения видно не было; разошлись бы они молча при первом же пакете,
объявленном только в файле: автор написал бы аннотацию, как велит сам файл, а
второй гейт её не прочитал бы и назвал модуль непоставленным — красное на
верной работе. Заодно ушли две записи словаря без предмета (`python-dateutil`,
`pillow`) и опечатка, которую он прятал: значение `"PIL"` не сравнивалось с
импортом ни разу, потому что сравнение идёт по нижнему регистру. Граница
названа: у пакета, поставленного прямо в шаге, объявления нет — берётся его
собственное имя, и угадывать здесь нечем; в прогонах каталога таких установок
сегодня нет вовсе.

> правило 022 — это его исполнение: две формулировки одной территории
> расходятся молча, и здесь их стало одной. Своей записи класс не требует
