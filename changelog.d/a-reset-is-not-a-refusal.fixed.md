Ответ потребителя переспрашивается, прежде чем объявляться нечитаемым: одна
попытка не отличает отказ площадки от обрыва связи, а прогон судил по ней.
Замер 8 сентября: ночной `consumers-sync` (прогон 34255883521) объявил двух
потребителей недоступными по «Connection reset by peer», покраснел и завёл
задачу #437 — а прогон значков через четверть часа прочитал обоих, и в сводке
у них стоит `read_at: 2026-09-08` и по 181 ответу. Теперь повторяется то, где
ответа НЕТ вовсе — обрыв, таймаут, отказ имени, 5xx, — и не повторяется то,
что ответом является: 404 говорит «файла нет», 403 — «не покажу», и второй
вопрос их не изменит (051). Чтение при этом стало одним на два сборщика:
своя копия в `collect_proposals.py` отличалась ровно тем, чего не хватило, и
разошлась бы дальше молча (022).
