Заведён гейт `scripts/check_derived.py`: производное, которым владеет прогон, в изменении не едет. Под охраной `export/where.md` и `export/where.json` — их пересобирает consumers-sync, вход у них чужой (ответы потребителей), и менялись они без нас. Замер 3 сентября: три конфликта за одну смену, все в этих файлах — изменения #280 и #285 против плановой пересборки #284. Границы: правка самого сборщика везёт новую сводку законно, ветка пересборки освобождена поимённо.

> правилом не становится: предмет записан 125 и 160, а запрет стоял в ядре дословно — не хватало ровно механизма. Запрет читался как «не правь текстом», и пересборка сборщиком под него не попадала
