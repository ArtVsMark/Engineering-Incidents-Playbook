Английское дерево больше не приписывает людям пол, которого нет в источнике:
запись 130 говорила «because **he** held the backlog in **his** head», а
русский оригинал — «связал их человек… потому что держал бэклог в голове», без
местоимения вовсе. Таких мест в `rules/en/*.md` было два, оба в одном файле,
теперь ноль; `check_locale.py` сверяет язык, а не смысл, и такую вставку не
видит — это названная граница гейта, а не упущение.
