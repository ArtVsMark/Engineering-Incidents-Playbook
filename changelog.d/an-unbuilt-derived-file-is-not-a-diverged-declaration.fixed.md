Ревизия деклараций в `check_bindings.py` больше не считает несобранное
производное разошедшейся декларацией. Замер на свежем клоне: ступень 0,
которую свод велит новому окну спрашивать ПЕРВОЙ командой, отвечала двумя
ложными находками — 105 и 174 законно называют `export/where.json`, а его в
общей ветке нет вовсе, его пересобирает прогон, — и три числа ступени 0, о
которых скрипт сам же объявляет «печатаются ВСЕГДА» (177), не печатались ни
одного. Тот же вызов после `aggregate_bindings.py` отвечал «чисто»: один
коммит, два вердикта, разница в окне (037). Освобождён поимённо названный путь
из `check_derived.OWNED_BY_JOB` — канон один и с причиной у каждой строки
(068, 022), — а соседнее имя, которого никто не собирает, остаётся находкой.
