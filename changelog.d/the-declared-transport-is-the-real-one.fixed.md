Транспорт к площадке теперь держится гейтом, а не прозой. Ответ каталога по
правилу 001 утверждал, что всё, кроме включения автомержа, переведено на REST —
и это было ложно: в дереве нашлось 22 живых вызова семейств `gh issue` и
`gh pr`, идущих через GraphQL, где одна операция стоит ~300 points из часовых
5000 против одного запроса REST. Все переведены; единственное настоящее
исключение — включение автомержа, у которого REST-адреса нет вовсе, — названо
поимённо в `.rules/transport.json`, как того и требует само правило.
