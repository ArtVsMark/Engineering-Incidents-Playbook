{
  "schema": "1.0",
  "_": "Закрытый список операций, у которых REST-эквивалента НЕТ. Требование списка — из самого правила 001: «GraphQL применяется, только если операция в REST физически отсутствует, и каждый такой случай назван поимённо». Держит его scripts/check_transport.py: адрес, которого здесь нет, становится находкой.",
  "allowed": [
    {
      "where": ".github/workflows/automerge.yml:380",
      "what": "gh pr merge --auto",
      "why": "включение автомержа — это мутация enablePullRequestAutoMerge, у неё нет REST-адреса вовсе. Названа в самом правиле 001 первой в списке исключений. СЛИЯНИЕ при этом REST умеет (PUT …/pulls/N/merge) и переведено: разные операции, разные ответы."
    }
  ]
}
