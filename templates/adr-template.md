# ADR-`NNNN` — `<решение одной строкой: что выбрали, а не о чём думали>`

- **Статус:** `Proposed` → `Accepted` / `Rejected` → `Superseded by ADR-XXXX`
- **Дата:** `ГГГГ-ММ-ДД`
- **Ретро-запись:** `да / нет` — если решение давно принято кодом, а записи не
  было, пишем задним числом и помечаем честно.

## Контекст

`<Что заставило решать. Факты и числа, а не намерения.>`

`<Здесь же — премиса, если решение опирается на утверждение о текущем состоянии.
Её проверяют ПЕРВОЙ: находка «такого у нас нет» разваливается чаще всего.>`

## Решение

`<Что делаем. Нумерованными пунктами, если их несколько — по ним потом будут
проверять реализацию.>`

## Альтернативы

Самая ценная часть записи: принятое решение видно в коде, а **невидимо именно
то, что рассматривали и не выбрали** — и возвращается в обсуждение именно оно.

- **A. `<вариант>`.** Плюс: `<…>`. Минус: `<конкретный минус, из-за которого не
  выбрали>`. Отклонено.
- **B. `<вариант>`.** Плюс: `<…>`. Минус: `<…>`. Отклонено.
- **C. `<вариант>` (выбрано).** `<чем окупается>`

**Отказ — тоже решение и тоже записывается.** «Не вводим общий интерфейс до
третьего случая» — полноценная запись: незаписанный отказ вернётся следующим
предложением.

## Последствия

- `<что становится проще>`;
- `<что становится дороже — обязательный пункт, иначе это реклама, а не решение>`;
- `<при каком изменении условий решение пересматривается>`.

## Миграция

`<Только если решение фазовое. Иначе раздел удаляется.>`

Переходный слой совместимости — отдельное решение со своей ценой и **датой
снятия**: без даты он бессрочен по построению и переживает тех, кто его вводил.

---

<!--
Как пользоваться (удалить при заполнении):

  • одна запись — одно решение; файл NNNN-краткий-слаг.md
  • изменение решения — НОВАЯ запись, которая помечает старую Superseded.
    Старую не редактируют: правка задним числом стирает не устаревшее знание,
    а ПРИЧИНУ ПЕРЕХОДА — единственное, ради чего запись и заводят
  • отклонённые записи не удаляются
  • запись отвечает на «почему тогда» и не меняется; спецификация отвечает на
    «как сейчас» и правится свободно. Это разные документы

Правила: 042 (решение с альтернативами), 043 (не правится задним числом),
044 (премиса проверяется первой), 093 (шов рано, обобщение с третьего),
094 (переходная заглушка делает миграцию вечной).
-->
