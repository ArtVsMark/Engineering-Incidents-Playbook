# `<Правило одной строкой: утверждение, а не тема>`

**Область.** `<Одна-три области через запятую — предметная классификация,
по ней собирается навигация в указателе. Словарь закрыт: список областей
в scripts/build_rules_index.py, новая дописывается туда осознанно и сразу
с описанием — область без описания сборку не проходит. В обоих деревьях
области должны соответствовать друг другу, иначе сборка тоже падает.>`

**Правило.** `<Два-три предложения. Что делать и чего не делать. Без «желательно»
и «по возможности» — правило либо действует, либо его нет.>`

**Переносится вне Claude Code.** <да · нет · частично> — <причина. Поле
НЕОБЯЗАТЕЛЬНОЕ: нет — и ладно, задним числом его не проставляют. Но если оно
есть, причина обязательна: ответ без причины не проверить и не оспорить, а
«частично» без неё вообще ничего не сообщает. README каталога предупреждает
«не копируйте целиком, половина заточена под агентские окна» — это поле делает
предупреждение выбираемым по записям.>

## Инцидент

`<Что сломалось. С числами, датами и конкретикой: «14 ложных обвинений подряд»,
«83 заявленных high → 16 подтверждённых», «четыре PR из девяти».>`

`<Что попробовали сначала и почему это не сработало. Неудачная первая починка
ценнее удачной второй: она объясняет, почему очевидное решение неверно.>`

## Почему

`<Механизм поломки, а не мораль. Читатель должен суметь предсказать следующий
случай, а не запомнить этот.>`

`<Здесь же — асимметрия цены, если она есть: какая из двух ошибок дороже и
почему. Обычно именно она и определяет форму правила.>`

## Практические границы

- `<что делать буквально>`;
- `<что при этом легко упустить>`;
- `<условие, при котором решение пересматривается>`.

## Применимость

**Работает** `<где именно>`.

**Не работает** `<где именно — и это обязательный раздел. Без него каталог
скопируют целиком, включая заведомо чужое.>`

**Признак нарушения:** `<наблюдаемый факт, по которому видно, что правило не
соблюдается>`.

## След

`<След обязан РАЗРЕШАТЬСЯ: задача вида владелец/репозиторий#номер либо
потребитель из реестра с названным артефактом — «владелец/репозиторий —
CLAUDE.md § Источники истины». Проза следом не считается: без разрешимого следа
запись за месяц превращается в «кто-то говорил, что так лучше».>`

Смежное: `<ссылки на соседние правила — правило, не связанное ни с чем, обычно
либо дубль, либо слишком общее>`.

---

<!--
Как пользоваться (удалить при заполнении):

  • имя файла — трёхзначный номер и короткий слаг латиницей: 042-decision-records.md
  • номера не переиспользуются даже после удаления
  • правило без инцидента — не правило, а предпочтение: в каталог не идёт
  • пересмотр — НОВАЯ запись, старая помечается заменённой; правка задним числом
    стирает причину перехода
  • указатель каталога пересобирается из файлов, а не правится руками
  • область хранится в самом правиле: производный файл не может быть
    хранилищем — регенерация обнуляет его молча (правило 125)

Полностью — rules/ru/120-how-to-run-a-rule-catalogue.md
-->
